# Flash Sale admin extensions

Phase 1A is backend-only. This directory is intentionally present because the
Medusa plugin builder always prepares an admin-extension entry point, even when
the plugin does not yet ship admin routes or widgets.
